module.exports.run = async (Bot,message,args) => {
const { Canvas } = require('canvas-constructor');
const fsn = require('fs-nextra');

let msg4 = message.author;
let userID = msg4.id;
let avatarURL = msg4.avatarURL;

}
module.exports.help = {
    name: "fsw3"
}
